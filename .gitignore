# Project markdown lives outside this repo (local-only) — never commit it
inkrun-md/

# macOS
.DS_Store

# local Python venv for tools/pdf-to-pages.py
tools/.venv/

# local Playwright browsers for headless viewer verification
tools/.pw-browsers/
